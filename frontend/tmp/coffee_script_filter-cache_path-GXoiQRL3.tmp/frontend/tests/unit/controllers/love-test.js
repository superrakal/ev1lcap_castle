import { moduleFor, test } from 'ember-qunit';
moduleFor('controller:love', {});

test('it exists', function(assert) {
  var controller;
  controller = this.subject();
  return assert.ok(controller);
});
